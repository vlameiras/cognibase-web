// This is where you'll store your OpenAI Assistant ID after creating it
// You can create an assistant through the OpenAI dashboard or via API
import { openaiConfig } from '../lib/config';

export const assistantId = openaiConfig.assistantId;
